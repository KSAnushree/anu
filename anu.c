#include <stdio.h>

// sum of two numbers
int main()
{
    int a, b;
    printf("enter a =");
    scanf("%d", &a);

    printf("enter b =");
    scanf("%d", &b);

    int sum = a + b;
    printf("%d is sum", sum n\);

    // product of two numbers
    int c, d;
    printf("enter c=");
    scanf("%d", &c);

    printf("enter d =");
    scanf("%d", &d);

    int pro = c * d;
    printf("pro is = %d", pro);

    return 0;
}